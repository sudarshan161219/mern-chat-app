// import { useEffect, useState } from "react";
// import axios from "axios";

const Chat = () => {

  return (
    <div>
      {/* {chats.map((chat) => (
        <div key={chat._id}>{chat.chatName}</div>
      ))} */}
      <h1>Chat</h1>
    </div>
  );
};

export default Chat;
